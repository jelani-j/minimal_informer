AWSTemplateFormatVersion: '2010-09-09'
Description: Bootstrap stack to create SSM parameter for Lambda versioning

Resources:
  LambdaVersionParameter:
    Type: AWS::SSM::Parameter
    Properties:
      Name: /MyApp/LambdaVersion
      Type: String
      Value: "initial-placeholder"